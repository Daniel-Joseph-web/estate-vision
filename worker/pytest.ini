[pytest]
# Tests import `main`, `config`, `auth` and `pipeline.*` as top-level modules,
# the same way uvicorn does when it runs from this directory.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
asyncio_mode = auto
markers =
    integration: hits a live worker over HTTP; skipped unless WORKER_URL is set
